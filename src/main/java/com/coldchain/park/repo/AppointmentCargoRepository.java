package com.coldchain.park.repo;

import com.coldchain.park.domain.AppointmentCargo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AppointmentCargoRepository extends JpaRepository<AppointmentCargo, Long> {
}
