# 宿主机映射端口（必填，由验证环境注入；本地自测可自行指定，如 3194）
CC_PUBLISH_PORT=3194

# PostgreSQL（仅 compose 内网访问，不发布到宿主）
POSTGRES_DB=coldchain
POSTGRES_USER=cold
POSTGRES_PASSWORD=coldpass

# 园区运营基准时间，便于演示“晚到/夜间排队”。
# 格式 yyyy-MM-dd'T'HH:mm:ss；留空 = 使用容器真实当前时间。
# 例如：PARK_CLOCK=2026-09-18T13:00:00
PARK_CLOCK=
